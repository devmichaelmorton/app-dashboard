# ------------------------------------------------------------------
# TEMPLATE — names only, no real values. Safe to commit.
# Copy to .env and fill in. The two Clerk keys must be IDENTICAL to the
# ones in recipes-manager/.env — same keys = same shared login.
# ------------------------------------------------------------------
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""

# Your Clerk user id (Clerk dashboard -> Users -> you -> User ID)
OWNER_USER_ID=""
